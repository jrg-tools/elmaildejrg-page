import type { Newsletter } from '@/lib/types';

export function Archive({
  // slug,
  // id,
  content,
  // blocks,
  // isPublished,
  // updatedAt,
  // createdAt,
}: Newsletter) {
  return (
    <section className="mt-12 codex-editor">
    </section>
  );
}
